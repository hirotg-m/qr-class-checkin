# スポーツ教室 出欠管理システム 要件定義書（ドラフト）

## 1. 概要

- **対象団体**：地域のスポーツ教室（1団体が複数のクラスを運営する想定）
- **対象活動**：団体が運営する複数のクラス。クラス数・クラス名は固定せず、任意に追加できる
- **目的**：クラスごとの参加登録・出欠を記録し、クラスごとに参加者を明確に区別できるシステムを構築する
- **利用者**：指導者・運営者のみ（保護者はQR経由での参加登録フォームのみ利用、ログイン不要）

## 2. システムアーキテクチャ

```
[S3 + CloudFront]  静的ホスティング（管理画面フロント）
        ↓
[API Gateway]      HTTP API
        ↓
[Lambda]           各エンドポイント処理
        ↓
[DynamoDB]         データ永続化
```

- 保護者向けのQR参加登録フォームも同じAPI Gateway/Lambda経由でDynamoDBに書き込む想定（保護者はログイン不要）

## 3. 機能要件

### 3.1 クラス管理
- クラスは複数登録可能。クラスごとにクラス名・説明を保持する
- クラスごとに独立したスケジュール・月次PIN・参加者名簿を持ち、参加記録もクラス単位で明確に区別する
- クラスごとに**対象学年**を設定できる（例：クラスAは小学生のみ、クラスBは小学5・6年のみ、クラスCは小学生・中学生）
- 管理者は管理画面からクラスの追加・編集・削除が可能（クラス名・説明・対象学年を含む。CRUD UI、8.1節の管理者ロール）

### 3.2 活動予定管理
- カレンダーでクラスごとの活動日を表示、日付タップでその日の参加者一覧へ
- 活動日・開始/終了時刻はクラスごとに管理する（`Schedule`）
- 管理者は管理画面から活動予定・月次PINの追加・編集・削除が可能（CRUD UI）

### 3.3 参加登録（QR自己受付）
- **保護者自己入力が基本**
  - クラスの入口にQRコード（クラス固定）＋月ごとの暗証番号（PIN）を掲示
  - 保護者がQR読み取り→学年／氏名を入力して送信（学年はそのクラスの**対象学年**の範囲内から選択する形式とし、対象外の学年は選べないようにする）
  - 受付可能時間：活動開始30分前〜終了時刻（`Schedule`の時刻から自動判定、手動ON/OFF不要）
  - そのクラスの参加者名簿と照合し、一致すれば来場記録、不一致なら新規登録
  - 表記ゆれ時は候補提示（確認画面）
- **指導者代理入力（フォールバック）**
  - 保護者不在・スマホ未所持・電波不良時に、指導者が同じ内容を代理入力
  - `inputBy`（本人／代理）を記録し、後から区別可能にする
- 入力項目は**学年・氏名のみ**（連絡先は含まない）
- 事前登録は不要（初参加もその場で登録）

### 3.4 参加履歴の閲覧
- 運営者向け：クラスごとの日別参加者一覧、参加者ごとの参加回数・直近の参加日、を閲覧できる

## 4. データ設計（DynamoDB、案）

| テーブル | PK | SK | 主な属性 |
|---|---|---|---|
| `Classes` | `CLASS#<classId>` | - | クラス名、説明、対象学年（配列） |
| `Schedule` | `CLASS#<classId>` | `DATE#<date>` | 開始時刻、終了時刻、会場 |
| `MonthlyPin` | `CLASS#<classId>#MONTH#<yyyy-mm>` | - | 月ごとの暗証番号 |
| `Participants` | `CLASS#<classId>` | `PARTICIPANT#<id>` | 氏名、学年、初回参加日 |
| `SessionLog` | `CLASS#<classId>#DATE#<date>` | `PARTICIPANT#<id>` | 新規/既存フラグ、来場時刻、`inputBy` |
| `LoginAttempts` | `IP#<ip>` | - | 失敗回数、TTL（8.2節のレート制限用） |

参加者はクラスごとに名簿を分ける（`Participants`のPKが`classId`単位）。同一人物が複数クラスに参加する場合も、クラスごとに別レコードとして扱い、クラスごとの参加者を明確に区別する。

## 5. 未決のオープン課題

| # | 課題 | 状況 |
|---|---|---|
| 1 | 参加記録には連絡先が紐づかない。緊急連絡手段の確保方法は別途検討 | 未決 |

## 6. 今後検討する論点（候補）

- クラスによっては会費徴収・会員名簿管理・活動記録や写真提出が必要になる可能性があるが、現時点ではスコープ外
- 権限管理（指導者／管理者の役割分け、クラスごとの閲覧範囲の制限）

## 7. 開発・運用要件

### 7.1 技術構成
- フロントエンド：React（SPA）、Node.js 22
- バックエンド：FastAPI、Python 3.12（Mangumアダプタ経由でLambda上で実行、API Gateway HTTP API経由で公開）
- インフラ構築：**AWS SAM**（`/infra/template.yaml`。Lambda関数、DynamoDBテーブル、S3、API Gateway等を定義し`sam deploy`で適用。Lambdaランタイムは`python3.12`）
- リポジトリ：GitHub（会員情報を扱うため、開発時はプライベート推奨。OSS公開時は本ファイルのような匿名化版を配置）
- CI/CD：GitHub Actions（**対象はフロントエンド・バックエンドのコードデプロイのみ**、インフラ変更は含まない）

### 7.2 リポジトリ構成（案）
```
/frontend        React
/backend         FastAPI + Lambdaハンドラ（Mangum）
/infra
  template.yaml         SAMテンプレート（初回・変更時に`sam deploy`で手動実行）
/.github/workflows
  ci.yml        PR時：lint・テスト（デプロイなし）
  deploy.yml    main push時：バックエンド・フロントエンドのコードデプロイのみ
```

### 7.3 CI/CDフロー
- **PR作成時**：バックエンドはpytest、フロントエンドはlint/build確認のみ
- **mainへのpush時**：
  1. `aws-actions/configure-aws-credentials`でOIDC経由の一時クレデンシャルを取得（長期アクセスキーはSecretsに保存しない）
  2. **deploy-backend**：`pip install -t package/`→zip化→`aws lambda update-function-code`で既存Lambda関数のコードのみ更新
  3. **deploy-frontend**：`npm run build`→`aws s3 sync`→（CloudFront使用時）`aws cloudfront create-invalidation`
- インフラのリソース作成・変更（テーブル追加、権限変更等）はCIに含めず、必要な時に`/infra/template.yaml`を`sam deploy`で手動実行する運用

### 7.4 コスト方針（目標：月額300円以下）
| 項目 | 内容 | 目安 |
|---|---|---|
| Lambda | 恒久無料枠（100万リクエスト/月）内 | 0円 |
| API Gateway (HTTP API) | この規模なら無料枠内〜数円 | 0〜数円 |
| DynamoDB | オンデマンド課金、少量利用 | 数円〜十数円 |
| S3（フロント配信） | 小規模 | 数円 |
| CloudFront | 低トラフィックのため無料枠内〜わずか | 0〜数円 |
| GitHub Actions | プライベートリポジトリでも無料枠（2,000分/月）内 | 0円 |
| **合計目安** | | **月30〜100円程度** |

- **独自ドメインは使用しない**：Route53ホストゾーンは月額$0.50程度（約75円）かかり予算を圧迫するため、CloudFrontのデフォルトドメインで運用する
- **環境分離は行わない**：dev/prod分離はリソース倍増によりコスト増となるため、当面は単一環境で運用する

## 8. 認証・権限要件（指導者向け画面）

### 8.1 認証方式
- ログイン：指導者共通の**8桁数字コード**（個人アカウントは持たない）。権限は**閲覧のみ**（カレンダー・参加者一覧の確認）だが、参加者の代理入力は例外として指導者コードでも可能
- ログイン：管理者用の**8桁数字コード**。閲覧に加え、クラス・活動予定・月次PINの追加・編集・削除が可能（CRUD UI）
- コードはそれぞれ**定期的に変更**する運用（更新頻度は別途決定）


### 8.2 保管・セキュリティ対策
- コードは**AWS Systems Manager Parameter Store（SecureString）**に保存し、コード内やログに平文で残さない
- ログイン試行のレート制限を実装（同一IPから3分間に10回失敗したら一時ロック。DynamoDBに失敗回数を記録しTTLで自動クリア）
- ログイン成功後は**JWTトークン**を発行し、以降のAPI呼び出しは`Authorization: Bearer <token>`で認可
  - 署名鍵もSSM Parameter Store（SecureString）で管理
  - ペイロードは最小限（`role: viewer`または`role: admin`、`exp`程度。個人アカウントを持たないため`sub`は固定値または省略）
  - **有効期限：8時間**（発行から8時間で失効、再ログインが必要）

### 8.3 画面構成
1. ログイン画面（8桁コード入力）
2. クラス選択画面（運営するクラス一覧から選択。管理者はクラスの追加・編集・削除ボタンを表示。編集項目はクラス名・説明・対象学年）
3. カレンダー画面（月表示、選択したクラスの`Schedule`の活動日をハイライト。管理者は活動予定の追加・編集・削除ボタンを表示）
4. 活動詳細画面（日付タップ後）
   - 開始・終了時刻
   - 参加者一覧（`SessionLog`＋`Participants`結合、氏名・学年・新規/既存・入力方法(本人/代理)を表示）
   - 代理入力ボタン
5. 月次PIN管理画面（管理者のみ：クラスごとの月次PINの設定・変更）

### 8.4 API設計（案）

閲覧系（指導者・管理者共通）：
- `POST /auth/login` … 8桁コード照合、JWTトークン発行
- `GET /classes` … クラス一覧取得
- `GET /classes/{classId}/schedule?month=yyyy-mm` … カレンダー表示用
- `GET /classes/{classId}/sessions/{date}` … 指定日の参加者一覧
- `POST /classes/{classId}/sessions/{date}/proxy` … 指導者による代理入力（指導者・管理者どちらのトークンでも呼び出し可）

編集系（管理者のみ）：
- `POST /classes` … クラスの新規作成
- `PUT /classes/{classId}` … クラスの編集
- `DELETE /classes/{classId}` … クラスの削除
- `POST /classes/{classId}/schedule` … 活動予定の追加
- `PUT /classes/{classId}/schedule/{date}` … 活動予定の編集
- `DELETE /classes/{classId}/schedule/{date}` … 活動予定の削除
- `PUT /classes/{classId}/pin/{yyyy-mm}` … 月次PINの設定・変更

### 8.5 未決事項
- コードの更新頻度（月次／年度ごと等）

## 9. 統計・分析要件（指導者向け画面）

### 9.1 参加人数推移グラフ
- 集計単位：クラスごと・活動日ごとの参加人数（`SessionLog`の件数をカウント）
- 内訳：**学年別**の人数内訳を表示（合計人数だけでなく、どの学年が増減しているか分かるようにする）
- 表示形式：積み上げ棒グラフ（横軸：活動日、縦軸：参加人数、学年ごとに色分け）
- 期間選択：過去3ヶ月／半年／1年などをプルダウンで切り替え

### 9.2 API設計（案）
- `GET /classes/{classId}/stats/participants?from=&to=` … 期間内の活動日ごとに学年別の参加人数内訳を返す
- レスポンス例：
```json
[
  { "date": "2026-06-01", "total": 18, "byGrade": { "1年": 3, "2年": 5, "3年": 4, "4年": 2, "5年": 3, "6年": 1 } }
]
```
- 集計はSessionLogをクラス・期間指定でクエリし、Lambda側で学年ごとに件数を集計（この規模なら事前集計テーブルは不要）

### 9.3 フロントエンド実装方針
- Reactで`recharts`を使用（Stacked Bar Chart）
